# docker-ubuntu-desktop

Pull repository from dockerhub
---------------
$ docker pull songfeng/docker-ubuntu-desktop

Clone from github and build docker
------------
$ git clone https://github.com/franksongfeng/docker-ubuntu-desktop.git
$ docker build -t docker-ubuntu-desktop .

Run docker
----------
$ docker run -ti --rm songfeng/docker-ubuntu-desktop

Connect VNC
-----------
Use Vnc viewer to connect with <IP>:5900
$ vncviewer <IP>:5900

Connect SSH
-----------
$ ssh root@<IP>
